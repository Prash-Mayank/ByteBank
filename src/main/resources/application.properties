<<<<<<< HEAD
# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/bytebank?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Mail Configuration (Gmail SMTP example)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your_gmail@gmail.com
spring.mail.password=your_app_password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Configurations
bytebank.jwt.secret=9a6763b4f177b47a114002ecde58d7e7e6005c2a11b6d5f479d20c5d5e5e6e3c
bytebank.jwt.expiration-ms=3600000

# Razorpay Test Credentials (simulated)
razorpay.key.id=rzp_test_mockKeyId123
razorpay.key.secret=mockSecretKey4567890

# Stripe Test Credentials (simulated)
stripe.api.key=sk_test_mockStripeKey123

# Server Port
=======
# ===== ByteBank — base configuration =====
spring.application.name=bytebank

# View resolution (JSP)
spring.mvc.view.prefix=/WEB-INF/templates/
spring.mvc.view.suffix=.jsp

# ----- Database (overridden by .env / docker-compose) -----
spring.datasource.url=jdbc:mysql://localhost:3306/bytebank?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD:changeme}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ----- JWT -----
jwt.secret=${JWT_SECRET:dev-only-secret-change-me-dev-only-secret}
jwt.expiration-ms=3600000

# ----- Mail (Gmail SMTP) -----
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ----- Razorpay -----
razorpay.key-id=${RAZORPAY_KEY_ID:}
razorpay.key-secret=${RAZORPAY_KEY_SECRET:}

# ----- AI: Gemini + Ollama -----
gemini.api-key=${GEMINI_API_KEY:}
gemini.model=gemini-1.5-flash
ollama.base-url=${OLLAMA_BASE_URL:http://localhost:11434}
ollama.model=llama3.2

# ----- Business rules -----
bytebank.transfer.per-txn-limit=100000
bytebank.transfer.daily-limit=200000
bytebank.auth.max-failed-attempts=5
bytebank.auth.lockout-minutes=15

# ----- Actuator (Docker health checks) -----
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

>>>>>>> 093ee2d (ByteBank V2 project stucture)
server.port=8080
